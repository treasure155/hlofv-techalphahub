<style>
        .header-section {
            position: relative;
            background-image: url('/images/about.jpg'); /* Replace with your header image */
            background-size: cover;
            background-position: center;
            color: white;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
            padding: 100px 0;
            text-align: center;
        }
        
        /* Overlay style */
        .header-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.621);  /* Dark overlay, adjust opacity for desired thickness */
            z-index: 1;  /* Ensure overlay is above the background but below the text */
        }
        
        .header-section h1, .header-section p {
            position: relative;
            z-index: 2;  /* Ensure text is above the overlay */
        }
        </style>


    <!-- Header Section -->
    <header class="header-section">
        <h1 class="display-4">Our Gallery</h1>
        <p class="lead">Explore memorable moments and impactful events from Honourable Ladies of Value Foundation. <br>
            Our gallery showcases the smiles, support, and service that define our mission.
            </p>
    </header>
